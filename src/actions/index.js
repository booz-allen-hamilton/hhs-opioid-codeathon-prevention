export const MENU_TYPE = 'MENU_TYPE';
export const menuType = (payload) => ({
  type: MENU_TYPE,
  payload
});

export const MENU_TIME = 'MENU_TIME';
export const menuTime = (payload) => ({
  type: MENU_TIME,
  payload
});

export const COUNTY = 'COUNTY';
export const county = (payload) => ({
  type: COUNTY,
  payload 
});